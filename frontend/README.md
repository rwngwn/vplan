# Frontend

## E2E Tests

Run Playwright tests with:

```bash
npm run e2e
```

Use the interactive runner with:

```bash
npm run e2e:ui
```

If browser binaries are not installed in the local environment, run:

```bash
npx playwright install
```
